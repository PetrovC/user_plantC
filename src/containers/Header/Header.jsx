export const Header = () => {
    // const isLogged

    return(
        <header>
            <img src="" alt="logo" className="logo mobile"/>
            <span>
                {/* isLogged && <>props.nom</> */}
            </span>
        </header>
    )
}